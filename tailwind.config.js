// // Minimal Tailwind config — adjust `content` to match files that use utility classes
module.exports = {
  content: [
    './index.html',
    './js/**/*.js',
    './styles/**/*.scss'
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};